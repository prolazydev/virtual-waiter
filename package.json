{
  "name": "virtual-waiter-main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix ./server\" \"npm run dev --prefix ./client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@prolazydev",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
